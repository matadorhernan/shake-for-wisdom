import { TouchSwipesDirective } from './touch-swipes.directive';

describe('TouchSwipesDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchSwipesDirective();
    expect(directive).toBeTruthy();
  });
});
